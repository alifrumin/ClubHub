<%= form_for @club do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <%= f.label :img_url %>
  <%= f.text_field :img_url %>

	<%= f.submit 'submit', class: 'enter' %>
<% end %>
